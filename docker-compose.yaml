version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./database.sqlite:/app/database.sqlite
      - ./uploads:/app/uploads
    environment:
      - PORT=3001
      - NODE_ENV=production
    restart: always
